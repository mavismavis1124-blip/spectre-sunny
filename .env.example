# Copy to .env and set real values (do not commit .env).
# Required: CODEX_API_KEY for real-time token search, prices, trending.
# Optional: others for fallbacks and AI/X/News.

# ========== REQUIRED for search, prices, trending ==========
# Codex API – real-time on-chain data (https://codex.io). Get key at https://codex.io
CODEX_API_KEY=your_codex_api_key_here

# ========== OPTIONAL ==========
# CoinGecko – fallback prices when Codex fails. Free: leave empty. Pro: set (e.g. CG-xxx).
COINGECKO_API_KEY=

# OpenAI – Monarch AI answers from crawled content. Set for /api/ai/answer (fallback if Claude fails).
OPENAI_API_KEY=

# Anthropic Claude – Monarch AI with tools (web_search, fetch_url). Preferred when set.
# https://docs.anthropic.com
ANTHROPIC_API_KEY=
# Optional: model override (default: claude-sonnet-4-20250514)
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# SerpAPI or Tavily – web_search tool for Monarch. Optional; without it, Claude uses context only.
# Tavily (recommended for AI agents): https://tavily.com
TAVILY_API_KEY=
# SerpAPI: https://serpapi.com
SERPAPI_KEY=

# CryptoPanic – news feed in Research Zone (cryptopanic.com). Get free key at https://cryptopanic.com/developers/
# When set, Research Zone uses CryptoPanic first; otherwise falls back to CryptoCompare.
CRYPTOPANIC_API_KEY=

# CryptoCompare – news feed fallback. Free tier works without key; set for higher limits.
# https://min-api.cryptocompare.com
CRYPTOCOMPARE_API_KEY=

# Twitter / X – fetch latest tweet for @token latest tweet. Bearer token from developer portal.
# https://developer.twitter.com
TWITTER_BEARER_TOKEN=

# Spectre Posting API – auth for POST /api/v1/posts and /api/v1/posts/dry-run (spectre_posting_skill).
# Optional: if unset, posting routes accept any Bearer/X-API-Key (dev only).
SPECTRE_POSTING_API_KEY=
